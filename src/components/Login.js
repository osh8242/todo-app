import LoginForm from './LoginForm';
import LoginTemplate from './LoginTemplate';

const Login = () => {
  return (
    <LoginTemplate>
      <LoginForm />
    </LoginTemplate>
  );
};

export default Login;
